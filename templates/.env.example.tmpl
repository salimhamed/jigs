# Copy to .env and keep credentials out of version control.
WORKFLOW_TARGET_WORLD=@workflow/world-postgres
WORKFLOW_POSTGRES_URL=postgres://jigs:jigs@localhost:{{POSTGRES_PORT}}/jigs

# Optional integrations; hello needs neither credential.
# Set these when adding a workflow such as the ship recipe.
# Only read when linear.identity.mode is "key": a Linear personal API key.
LINEAR_API_KEY=
# Only read when linear.identity.mode is "app": the Linear OAuth application's
# client credentials. jigs mints its own token from them.
LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=
# Only read when github.identities is [{ mode: "pat" }]; an App mints its own tokens.
GITHUB_TOKEN=
# Required by workflows that use models.openrouter().
# OPENROUTER_API_KEY=

# Needed only when webhooks.linear is enabled in jigs.config.ts: the signing
# secret of the org-level Linear webhook.
LINEAR_WEBHOOK_SECRET=

# Needed only when webhooks.github is enabled in jigs.config.ts. Signs the
# GitHub webhook deliveries jigs bind registers; jigs never generates it.
# Create one with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=
