# The jigs packages will come from GitHub Packages under this scope. The
# token that reads them lives in ~/.npmrc, never here:
#   //npm.pkg.github.com/:_authToken=<a token with read:packages>
@salimhamed:registry=https://npm.pkg.github.com
