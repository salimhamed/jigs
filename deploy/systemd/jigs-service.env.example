# Copy to ~/.config/jigs/service.env (referenced by jigs-service.service).
WORKFLOW_TARGET_WORLD=@workflow/world-postgres
WORKFLOW_POSTGRES_URL=postgres://jigs:jigs@localhost:5439/jigs
PORT=8990

# Consumed by trigger-path preflight from ADR 0010 onward (AGE-315).
# This ticket only provisions the slots; nothing reads them yet.
LINEAR_API_KEY=
GITHUB_TOKEN=
